/*!
 * perfect-scrollbar v1.5.5
 * Copyright 2021 Hyunje Jun, MIT license
 */
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global = global || self, global.PerfectScrollbar = factory());
}(this, (function () { 'use strict';

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }
})));

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }

    var style = get(element);
    return style;
  }

  function get  (element) {
    return getComputedStyle(element);
  }

  function getComputedStyle(element) {
    if (element === null) {
      return null;
    }
    var style = get(element);
          return style;
  }
